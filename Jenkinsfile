@Library('jenkins-pipeline-shared-libraries@DEVOPS-2392') _

pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                sh 'env'
                
            }
        }
    }
}